<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:bean="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://code.alibabatech.com/schema/dubbo
            http://code.alibabatech.com/schema/dubbo/dubbo.xsd
            http://www.springframework.org/schema/context
            https://www.springframework.org/schema/context/spring-context.xsd">
    <!-- 扫描包，通过找到标记的注解，进行bean初始化 -->
    <bean:component-scan base-package="com.darben" />

    <!-- dubbo可以与spring无缝整合-->
    <!-- 1、
         1.1 配置别名，目的可以在后台可以根据别名监控这个服务状态，方便识别 -->
    <dubbo:application name="product-provider" owner="programmer" />
    <!-- 1.2 配置端口，提供服务ip+端口，外部才能连接访问
         注意：端口不能重复和被其他应用占用
    -->
    <dubbo:protocol name="dubbo" port="7788" />

    <!--2、配置注册中心
        protocol:注册中心的协议格式
        address:注册中心的地址
     -->
    <dubbo:registry address="192.168.43.175:12181,192.168.43.175:12182,192.168.43.175:12183"
                    protocol="zookeeper" />

    <!-- 3、发布的服务
        interface:代表发布服务的类型，就是接口
        ref:代表具体发布的服务，接口实现类
        timeout:连接超时的时间,单位毫秒
    -->
    <dubbo:service interface="com.darben.service.ProductService"
                   ref="productServiceImpl"
                   timeout="60000" />
</beans>